apiVersion: v1
kind: Service

metadata:
  name: {{ include "shop-admin.fullname" . }}
  labels:
    {{- include "shop-admin.labels" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}

  selector:
    {{- include "shop-admin.selectorLabels" . | nindent 4 }}

  ports:
  {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
  {{- end }}